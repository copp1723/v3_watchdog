name: "Disabled Workflow"
# This workflow has been disabled and merged into ci.yml
# Kept as an empty file to override any previous versions in Git history

on:
  # This prevents this workflow from running
  workflow_dispatch:
    
jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is intentionally disabled" 